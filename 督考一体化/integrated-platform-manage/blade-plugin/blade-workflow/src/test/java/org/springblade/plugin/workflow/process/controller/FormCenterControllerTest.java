/*
package org.springblade.plugin.workflow.process.controller;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springblade.core.test.BladeBootTest;
import org.springblade.core.test.BladeSpringRunner;
import org.springblade.plugin.workflow.design.controller.FormCenterController;
import org.springblade.plugin.workflow.design.entity.WfForm;
import org.springframework.beans.factory.annotation.Autowired;

import javax.annotation.Resource;

import static org.junit.Assert.assertTrue;

*/
/**
 * @author : luye
 * @date : 2021/8/6
 *//*

@RunWith(BladeSpringRunner.class)
@BladeBootTest(appName = "blade-workflow", profile = "dev")
public class FormCenterControllerTest {

	@Resource
	private FormCenterController formCenterController;

	@Test
	public void testGetForm(){
		WfForm form = new WfForm();
		form.setFormKey("leave");
		form.setVersion(1);
		assertTrue(formCenterController.getForm(form).isSuccess());
	}
}
*/
