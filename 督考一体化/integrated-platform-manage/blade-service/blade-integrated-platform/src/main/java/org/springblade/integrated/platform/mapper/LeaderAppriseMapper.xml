<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.springblade.integrated.platform.mapper.LeaderAppriseMapper">

    <!--<select id="tree" resultMap="treeNodeResultMap">
        select id, parent_id, dict_value as title, id as "value", id as "key" from blade_dict_biz where is_deleted = 0
    </select>-->


<select id="exportLeaderApprise" resultType="org.springblade.integrated.platform.excel.LeaderAppriseExcel">
    <!-- 如有需要可以加上条件 where id= #{qw.id} -->
    select dept_name, (case satisfaction when '1' then '满意' when '2' then '比较满意'  when '3' then '基本满意' when '4' then '不满意' end) satisfaction
    ,apprise_leader ,score ,apprise_content  from leader_apprise where is_deleted=0
</select>

</mapper>
